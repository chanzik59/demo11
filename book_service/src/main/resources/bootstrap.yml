spring:
  application:
    name: book-service
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yml
        refresh-enabled: true
#      discovery:
#        cluster-name: SH








spring:
  application:
    name: user-service
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yml
        refresh-enabled: true
#      discovery:
#        cluster-name: BJ






